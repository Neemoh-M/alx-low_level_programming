#include "main.h"

/**
 * main- 1_is_digit return value 1 through 9
 *
 * @c- integer
 *
 * Return: Returns 1 if c is digit and 0 if else
 */
int _isdigit(int c);
	{
		if(c >= '0' && c <= '9')
		Return (1);
		else
		Return(0);
	}
